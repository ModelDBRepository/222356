To save disk space the redundant breath traces were not written in
large parallel simulations.  This code will write the missing breath
traces so that data analysis can proceed normally as was done with the
experimental data.
